% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clock_hand.R
\name{derive_hand_dynamics}
\alias{derive_hand_dynamics}
\title{Clock Hand Dynamics}
\usage{
derive_hand_dynamics(
  metadata_df,
  events_df,
  cfg = config::get(),
  sampling_rate_hz = 500
)
}
\arguments{
\item{metadata_df}{A tibble with participant metadata containing:
id, n_trials (to infer duration)}

\item{events_df}{A tibble with events containing signal timing information}

\item{cfg}{Configuration list (from config::get()) containing clock parameters}

\item{sampling_rate_hz}{Sampling rate in Hz for output (default: 500)}
}
\value{
A tibble with columns:
\itemize{
\item id: Participant identifier
\item t: Time in milliseconds
\item angle_deg: Angular position of hand in degrees (0 = top, increases clockwise)
\item tip_x_px: X coordinate of hand tip in pixels
\item tip_y_px: Y coordinate of hand tip in pixels
\item center_x_px: X coordinate of clock center
\item center_y_px: Y coordinate of clock center
}
}
\description{
Computes the x,y pixel coordinates of the clock hand tip at each time point
based on angular position, hand length, and clock center. This replicates
the hand dynamics from \code{matlab/task.m} lines 219-224.
}
\details{
Functions for computing the moving clock hand's tip position over time
based on task parameters from matlab/task.m
Derive clock hand tip positions over time

The clock hand moves in discrete steps based on task.m:
\itemize{
\item \strong{Circle diameter}: diameter_ratio * min(screen_width, screen_height)
(task.m line 171-172: circleratio = 1/3)
\item \strong{Hand length}: hand_length_ratio * diameter
(task.m line 221: linelength = dmtr/2 - dmtr*0.05 = 45\% of diameter)
\item \strong{Angular increment}: increment_deg per step
(task.m line 222: incrmnt = 3.41 degrees)
}

Hand position calculated using:
\itemize{
\item tip_x = center_x + hand_length * sin(angle)
\item tip_y = center_y - hand_length * cos(angle)
(task.m line 224: sine/cosine with negative y for clockwise rotation)
}

The negative cos gives clockwise rotation starting from top (12 o'clock).
}
\examples{
\dontrun{
cfg <- config::get()
metadata <- read_mat_data("file.mat")$metadata
events <- read_mat_data("file.mat")$events
hand_positions <- derive_hand_dynamics(metadata, events, cfg)
}

}
