% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_analysis.R
\name{join_image_content}
\alias{join_image_content}
\title{Image Content Analysis Integration}
\usage{
join_image_content(trials_df, image_analysis_df, mismatch_tolerance = 0.01)
}
\arguments{
\item{trials_df}{A tibble with trial-segmented data (from segment_trials)
containing: id, trial, image_index, and gaze metrics}

\item{image_analysis_df}{A tibble with image analysis results containing:
\itemize{
\item image_id or image_index: Image identifier (matches trial data)
\item arousal: Arousal rating (numeric)
\item valence: Valence rating (numeric)
\item category: Content category (character/factor)
\item (other optional attributes)
}}

\item{mismatch_tolerance}{Maximum proportion of trials allowed to have
missing image data (default: 0.01 = 1\%)}
}
\value{
A tibble with trial data augmented with image attributes:
\itemize{
\item arousal: Image arousal rating
\item valence: Image valence rating
\item category: Image content category
\item (all original trial columns)
}
}
\description{
Merges trial-segmented eye-tracking data with image analysis outputs
(arousal, valence, content categories) produced by the csn-image-analysis
repository using shlab.imgct.
}
\details{
Functions for merging eye-tracking data with image analysis outputs
from the csn-image-analysis repository (arousal, valence, categories)
Join image content attributes to trial data

Image analysis attributes enable stratified analyses of on-task behavior:
\itemize{
\item \strong{Arousal tertiles}: Low, medium, high arousal images
\item \strong{Valence bands}: Negative, neutral, positive valence
\item \strong{Content categories}: e.g., faces, scenes, objects
}

The function enforces a mismatch tolerance: if more than \code{mismatch_tolerance}
proportion of trials lack image data, it errors out (suggesting data quality
issues). Missing image data could indicate:
\itemize{
\item Image index mismatch between behavioral and image analysis
\item Incomplete image analysis processing
\item Image set version differences
}
}
\examples{
\dontrun{
# Load image analysis results
image_data <- readr::read_csv("path/to/image_analysis.csv")

# Join to trials
trials_with_images <- join_image_content(
  trials,
  image_data,
  mismatch_tolerance = 0.01
)

# Analyze by arousal tertile
trials_with_images |>
  mutate(arousal_tertile = ntile(arousal, 3)) |>
  group_by(arousal_tertile, t_trial_ms) |>
  summarize(p_on_task = mean(on_task, na.rm = TRUE))
}

}
