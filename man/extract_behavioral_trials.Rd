% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/behavioral.R
\name{extract_behavioral_trials}
\alias{extract_behavioral_trials}
\title{Extract Trial-Level Behavioral Data}
\usage{
extract_behavioral_trials(subjdata, participant_id)
}
\arguments{
\item{subjdata}{The subjdata structure from .mat file (after array extraction)}

\item{participant_id}{Participant identifier}
}
\value{
A tibble with one row per trial containing:
\itemize{
\item \code{trial}: Trial number (1 to nTrials)
\item \code{id}: Participant identifier
\item \code{p_signal}: Probability of signal trials (session constant)
\item \code{clock_side}: 0 (left) or 1 (right) - session constant
\item \code{signal_flag}: 1 if signal occurred this trial, 0 otherwise
\item \code{signal_time}: Time of signal in ms (or NA if no signal)
\item \code{response_flag}: 1 if participant responded this trial, 0 otherwise
\item \code{response_time}: Time of response in ms (or NA if no response)
\item \code{image_index}: Which image was shown (1-10 for synthetic, 1-N for real)
\item \code{task_begin}: Experiment start time
\item \code{task_end}: Experiment end time (or current time during session)
}
}
\description{
Creates a trial-level behavioral dataframe from subjdata, with one row per
trial containing all behavioral information: signals, responses, images, etc.
This is distinct from the events stream and is needed for behavioral analyses.
}
\details{
This function replicates the structure from the original \code{extract_behavioral_data()}
but with modernized tidyverse conventions. It handles both \code{steps} and \code{step}
field variants.

For behavioral modeling (PR-C), we'll derive:
\itemize{
\item Hits: signal_flag==1 & response_flag==1 & (response_time - signal_time) < 8000ms
\item Misses: signal_flag==1 & response_flag==0
\item False Alarms: signal_flag==0 & response_flag==1
\item Correct Rejections: signal_flag==0 & response_flag==0
\item Reaction Times: response_time - signal_time (for hits only)
}
}
\examples{
\dontrun{
mat_data <- read_mat_data("path/to/file.mat")
behavioral_df <- extract_behavioral_trials(mat_data$subjdata, "CSN001")
}

}
\keyword{internal}
