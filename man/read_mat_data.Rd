% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mat_data.R
\name{read_mat_data}
\alias{read_mat_data}
\title{Read Eye-Tracking and Behavioral Data from MATLAB .mat Files}
\usage{
read_mat_data(path, participant_id = NULL)
}
\arguments{
\item{path}{Character path to the \code{.mat} file.}

\item{participant_id}{Optional participant identifier. If NULL, attempts to
extract from filename (e.g., "CSN001" from "CSN001_data.mat").}
}
\value{
A list with four tibbles:
\itemize{
\item \code{samples}: High-frequency gaze samples (30,000 rows) with columns \code{id},
\code{t}, \code{x_px}, \code{y_px}, \code{pupil}, \code{eye}, \code{blink}, \code{inside_screen}.
\item \code{events}: Discrete events (132 rows) with columns \code{id}, \code{type}, \code{t}, \code{msg},
and parsed validation/behavioral fields when applicable.
\item \code{behavioral}: Trial-level behavioral data (60 rows) with columns \code{trial},
\code{id}, \code{signal_flag}, \code{signal_time}, \code{response_flag}, \code{response_time},
\code{reaction_time}, \code{image_index}, \code{clock_side}, \code{outcome}, etc.
\item \code{metadata}: Session-level metadata (1 row) including sampling rate,
duration, trial count, clock position, etc.
}
}
\description{
Reads \code{.mat} files containing eye-tracking samples and behavioral responses
from the CSN study. The files are expected to contain two top-level structures:
\itemize{
\item \code{subjdata}: behavioral data (trials, responses, signals)
\item \code{Edf2Mat}: eye-tracking data (samples, events, recordings)
}
}
\details{
The function handles both \code{steps} and \code{step} field variants in \code{subjdata}.
Times in the \code{.mat} file are in milliseconds and are preserved as-is.
Gaze positions from the second row of \code{gx}/\code{gy} matrices (right eye) are
extracted; the first row (left eye) contains NaNs for monocular recording.
}
\examples{
\dontrun{
mat_file <- system.file("extdata/synthetic/synthetic_01.mat",
                        package = "itrackvalr")
data <- read_mat_data(mat_file)
str(data, max.level = 1)
# List of 4
#  $ samples   : tibble [30,000 × 8]  - Gaze samples
#  $ events    : tibble [132 × 15]    - Event stream
#  $ behavioral: tibble [60 × 15]     - Trial-level behavioral
#  $ metadata  : tibble [1 × 11]      - Session metadata
}

}
