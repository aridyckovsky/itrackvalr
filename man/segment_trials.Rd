% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{segment_trials}
\alias{segment_trials}
\title{Segment binarized data into image-presentation trials}
\usage{
segment_trials(samples_df, events_df, behavioral_df)
}
\arguments{
\item{samples_df}{A tibble (not LazyFrame - this requires materialization)
with binarized samples containing: id, t, on_task, and other columns}

\item{events_df}{A tibble with events containing image_onset and image_offset}

\item{behavioral_df}{A tibble with trial-level behavioral data containing:
id, trial, image_index, task_begin, task_end}
}
\value{
A tibble with columns:
\itemize{
\item id: Participant identifier
\item trial: Trial number
\item t_trial_ms: Time within trial (0 = image onset)
\item t_absolute: Absolute timestamp
\item on_task: On-task indicator
\item image_index: Which image was presented
\item (all other columns from samples_df)
}
}
\description{
Slices binarized gaze data into trials based on image onset/offset events.
Each trial includes all gaze samples during that image presentation period,
with time normalized relative to trial start.
}
\details{
Trial segmentation:
\enumerate{
\item Extract image_onset and image_offset events
\item For each trial, select samples between onset and offset
\item Normalize time to trial start (t_trial_ms = 0 at onset)
\item Join trial metadata (image_index, behavioral outcomes)
}

This enables trial-level analyses and timecourse plots relative to
image onset (e.g., p(on-task) from 0-1000 ms post-onset).

\strong{Note}: This function requires materialized data (not LazyFrame) because
trial segmentation involves complex per-trial operations. If input is a
LazyFrame, it will be computed to tibble first.
}
\examples{
\dontrun{
samples_binarized <- binarize_on_task(samples, zone_radii)
trials <- segment_trials(samples_binarized, events, behavioral)

# Analyze on-task timecourse per trial
trials |>
  group_by(trial, t_trial_ms) |>
  summarize(p_on_task = mean(on_task, na.rm = TRUE))
}

}
