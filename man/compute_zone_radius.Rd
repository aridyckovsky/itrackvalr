% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{compute_zone_radius}
\alias{compute_zone_radius}
\title{Compute zone-of-uncertainty radius from validation errors}
\usage{
compute_zone_radius(
  validation_df,
  strategy = c("average", "maximum", "pre_avg", "post_avg"),
  scaling = 1,
  cfg = config::get()
)
}
\arguments{
\item{validation_df}{A tibble with validation data (from parse_validation_msgs)
containing: id, type ("pre"/"post"), avg_err_deg, max_err_deg}

\item{strategy}{Strategy for computing radius:
\itemize{
\item "average": Use average validation error across pre/post (default)
\item "maximum": Use maximum validation error (conservative)
\item "pre_avg": Use only pre-task average error
\item "post_avg": Use only post-task average error
}}

\item{scaling}{Scaling factor to apply to the computed radius (default: 1.0)
Values >1 make the zone more lenient, <1 more strict}

\item{cfg}{Configuration list (from config::get()) for deg_to_px conversion}
}
\value{
A tibble with columns:
\itemize{
\item id: Participant identifier
\item r_px: Zone radius in pixels
\item r_deg: Zone radius in degrees (before conversion)
\item strategy: Strategy used
\item scaling: Scaling factor applied
}
}
\description{
Calculates the radius (in pixels) of the zone of uncertainty around the
clock hand tip, based on calibration validation errors. This radius
defines the threshold for determining whether a gaze point is "on-task"
(looking at the clock hand) or "off-task".
}
\details{
The zone of uncertainty represents the expected positional error in gaze
tracking. A larger zone is more lenient (more samples classified as on-task),
while a smaller zone is more strict.

Typical values:
\itemize{
\item Average error: ~0.5-1.5 degrees → ~18-54 pixels
\item Maximum error: ~2-4 degrees → ~72-144 pixels
}

For sensitivity analyses, vary the \code{scaling} parameter (e.g., 0.5, 1.0, 1.5)
to assess how zone size affects on-task estimates.
}
\examples{
\dontrun{
validation_df <- parse_validation_msgs(events)
zone_radii <- compute_zone_radius(validation_df, strategy = "average", scaling = 1.0)
# Use in binarization
samples_binarized <- binarize_on_task(samples, hand_positions, zone_radii)
}

}
