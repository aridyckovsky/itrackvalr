% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_polars.R
\name{sink_parquet}
\alias{sink_parquet}
\title{Stream LazyFrame directly to Parquet file}
\usage{
sink_parquet(lf, path, compression = "snappy")
}
\arguments{
\item{lf}{A polars LazyFrame}

\item{path}{Output file path (must end in .parquet)}

\item{compression}{Compression method ("snappy", "gzip", "brotli", "lz4", "zstd", "uncompressed")}
}
\value{
Invisibly returns the output path
}
\description{
Writes a polars LazyFrame directly to a Parquet file without loading
the full dataset into memory. This is the preferred method for exporting
large datasets (>1M rows).
}
\details{
Streaming export with \code{sink_parquet()} enables processing datasets larger
than available RAM. For the full CSN cohort (90M samples), this:
\itemize{
\item Uses <2 GB RAM vs >10 GB for in-memory operations
\item Writes directly to disk without materialization
\item Applies all lazy transformations during the stream
}
}
\examples{
\dontrun{
samples_lf <- to_polars_lf(all_samples) |>
  filter(eye == "RIGHT") |>
  mutate(x_px_adj = x_px + offset_x)

# Stream to disk without loading into memory
sink_parquet(samples_lf, "output/extracted/ALL_samples.parquet")
}

}
