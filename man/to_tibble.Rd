% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_polars.R
\name{to_tibble}
\alias{to_tibble}
\title{Convert polars LazyFrame to tibble}
\usage{
to_tibble(lf)
}
\arguments{
\item{lf}{A polars LazyFrame}
}
\value{
A tibble
}
\description{
Converts a polars LazyFrame back to a tibble by computing the lazy query.
This materializes the data into memory.
}
\details{
Use this when you need to:
\itemize{
\item Materialize results for interactive inspection
\item Pass data to functions that don't support LazyFrames
\item Work with small enough datasets that fit in memory
}

For large datasets (>1M rows), prefer \code{sink_parquet()} to stream directly
to disk instead of loading into memory.
}
\examples{
\dontrun{
samples_lf <- to_polars_lf(samples) |>
  filter(eye == "RIGHT")
samples_tibble <- to_tibble(samples_lf)
}

}
