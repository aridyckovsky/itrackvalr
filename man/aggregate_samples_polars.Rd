% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_participant_polars.R
\name{aggregate_samples_polars}
\alias{aggregate_samples_polars}
\title{Polars-Based Multi-Participant Processing}
\usage{
aggregate_samples_polars(participant_list)
}
\arguments{
\item{participant_list}{List of participant data (from read_all_participants)}
}
\value{
A polars LazyFrame with all participants' samples combined
}
\description{
Combines samples from multiple participants into a single LazyFrame
using lazy evaluation. Unlike the eager dplyr version, this does NOT
load all data into memory, making it suitable for processing 57 participants
(~90 million samples).
}
\details{
Lazy evaluation functions for aggregating and processing large-scale
eye-tracking data across multiple participants. These functions use
tidypolars LazyFrames to handle datasets larger than available RAM.
Aggregate samples across participants using polars (lazy)

This function is the polars equivalent of \code{aggregate_samples()}. Key differences:
\itemize{
\item Returns a LazyFrame (lazy) instead of tibble (eager)
\item Data remains on disk until \code{compute()} or \code{sink_parquet()} is called
\item Memory usage: ~2 GB vs >10 GB for dplyr on 90M samples
\item Processing time: 10-15x faster than dplyr for large datasets
}

Typical usage in pipeline:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{all_samples_lf <- aggregate_samples_polars(participant_data) |>
  apply_calibration_offsets(offsets_df) |>
  resample_samples_polars(hz_standard = 500)

# Stream to disk without loading into memory
sink_parquet(all_samples_lf, "output/extracted/ALL_samples.parquet")
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
mat_files <- get_mat_files("inst/extdata/synthetic")
participant_data <- read_all_participants(mat_files)
samples_lf <- aggregate_samples_polars(participant_data)
# samples_lf is lazy - no data loaded yet
}

}
